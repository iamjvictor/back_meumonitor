import { StudentPurchaseController } from '../../../controllers/student-purchase.controller.js';

/** Controller HTTP do checkout. A implementação legada permanece como compatibilidade. */
export class CheckoutController extends StudentPurchaseController {}
